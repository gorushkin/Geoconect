import { useClient } from '../../hooks';
import Layout from '../../components/Admin/Layout';
import { Container, Row } from 'react-bootstrap';

const AddNews = () => {

  return (
    <Layout title='Add newsu' closed>
      <div></div>
    </Layout>
  );
};

export default AddNews;
