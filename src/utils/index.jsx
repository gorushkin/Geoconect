import { useClient } from '../hooks';

export const WithClientRenderWrapper = ({ children }) => {
  // const isClient = useClient();

  // return isClient ? { children } : null;
  return <h1>sdfasdfsd</h1>;
};
